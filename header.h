#ifndef H_FILE
#define H_file
/*standard header file*/
#include <stddef.h>
#include <unistd.h>

/*functions pothotype*/
int _putchar(char);
int _printf(const char *, ...);
int _print_s(char *);/* the function prints the required string of an arg and return the lengt of the string printed, to be used when in a loop*/

#endif
